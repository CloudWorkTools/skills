interface:
  display_name: "PR Preview"
  short_description: "Production-like pull request previews for human review"
  default_prompt: "Use $pr-preview to create or verify an isolated preview of this PR using the project's production build path."
